spring.application.name=ecommerce

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ecommerce_db
spring.datasource.username=noman
spring.datasource.password=noman123
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT secret key (use a strong secret, ideally at least 256-bit key base64-encoded for HMAC)
jwt.secret=83Tx2971QkFDB3h755XMMe/n8HF7TDkPpViVH/4bSNc=

# Generate a 256-bit key and encode it in Base64 via terminal
# openssl rand -base64 32


# JWT expiration time in milliseconds (e.g., 1 hour)
jwt.expirationMs=3600000